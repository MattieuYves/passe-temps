<%= simple_form_for [@skill, @booking] do |f| %>
  <%= f.input :start_date, input_html: { data: { controller: 'flatpickr' } } %>
  <%= f.input :end_date, input_html: { data: { controller: 'flatpickr' } } %>
  <%= f.submit "Create booking" %>
<% end %>
