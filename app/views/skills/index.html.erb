<div class="container">
    <div class="container-wrap">
        <div class="card-display-content">
            <div>
                <h1>See all the teachers</h1>
            </div>
            <%= render "shared/searchbar" %>
            <div class="index-card">
                <% if @users.any? %>
                    <% @users.each do |user| %>
                    <%= render "shared/card", user: user %>
                <% end %>
                <% else %>
                    <p>No users found.</p>
                <% end %>
            </div>
        </div>
        <div class="maps">

        </div>
    </div>
<div>