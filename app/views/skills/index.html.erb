<div class="container">
    <div class="row col-6">
        <div>
            <h1>See all the teachers</h1>
        </div>
    </div>
    <div class="container-wrap">
        <div class="row mt-2 mb-2 justify-content-evenly" style>
            <% if @users.any? %>
                <% @users.each do |user| %>
                <%= render "shared/card", user: user %>
            <% end %>
            <% else %>
                <p>No users found.</p>
            <% end %>
        </div>
        <div class="maps">

        </div>
    </div>
<div>